# 项目依赖说明

## Uvicorn 
Uvicorn 是专门运行 Python 异步 Web 应用 的「高性能服务器」—— 你可以把它理解为：FastAPI 负责定义接口、处理业务逻辑（比如 LCAI 项目里的表单生成、LangGraph 流程、/lcai/invoke 接口），但它本身没法直接 “监听网络端口、接收前端请求”；而 Uvicorn 就是帮 FastAPI 搭起 “网络桥梁” 的工具：前端的请求先到 Uvicorn，Uvicorn 转交给 FastAPI 处理，再把处理结果返回给前端。